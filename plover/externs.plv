import prelude;

extern (
  puts (x :: string) :: ();
);

newline () :: () := puts "";

print_matrix {n, m} (A :: double[n,m]) :: ()
  := ( for i in n -> (for j in m -> printf "%f " A[i,j]; newline()) );
